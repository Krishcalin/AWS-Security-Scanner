# OverWatch — scanner CORE runtime (the pure engine; everything else is stdlib).
# Fully pinned. Regenerate the offline wheelhouse with scripts/build_offline_bundle.sh
# on a connected host (pip download resolves + hashes the transitive deps).
boto3==1.40.51
botocore==1.40.51
